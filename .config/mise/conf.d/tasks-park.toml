[tasks."park:add"]
description = "Add a Park UI component to the demo"
usage = '''
arg "<name>..." help="Name of the component to add, e.g. 'button'"
'''
run = "mise run //apps/demo:park:add -- ${usage_name?}"

[tasks."park:fix"]
description = "Apply Park UI component adaptations in the demo"
run = "mise run //apps/demo:park:fix"
